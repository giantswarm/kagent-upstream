# The Giant Swarm kagent line

This repository is the kagent the [Giant Swarm Agent Platform](https://github.com/giantswarm/agent-platform)
runs: upstream [kagent-dev/kagent](https://github.com/kagent-dev/kagent) `main` at a pinned commit, plus the
patches the platform cannot run without until they are merged upstream. Team Bumblebee owns it. The line
exists because the platform moved to the kagent API v2 (`kagent.dev/v1alpha3`, Substrate actors) before
upstream has released it; the day upstream ships a release that contains every carried patch, the platform
switches to that release and this line ends.

| | |
|---|---|
| Consumed branch (and default branch) | `giantswarm` (the same name as the Substrate line's) — what the release tags are cut from, and what the platform's dev channel and agentlab follow between releases |
| Mirror | `main`, every `release/v*.x` branch and every tag — read-only mirrors of upstream, fast-forwarded by the sync workflow, never edited (upstream's CI reads release branches and tags to pick the upgrade-test baseline) |
| Maintenance branch | `release-1.0` — the 1.0 line, for installations whose kagent still speaks the v0.0.29 ate-api contract (the agent-platform meta chart's `>=1.0.0 <1.1.0`): `v1.0.1` plus what a 1.0.x patch needs, cut on 2026-09-23 because `giantswarm` moved to upstream `main@844ea06b` and kagent-dev/substrate v0.2.0-beta4 the day before, a contract change the Substrate line's `release-1.0` makes the same way. Pull requests against it are rebase-merged like the consumed branch's and run the same suites; its releases are `v1.0.Z` tags on it, published by the same pipeline (a push to it validates, only its tags publish); every fix it carries is also carried on `giantswarm`. Retired once the fleet runs the 1.1 line |
| Automation branches | `sync/**` (re-pin candidates), `ledger` (machine-written records) — never commit to them by hand |
| Upstream pin | `git merge-base giantswarm main` — the newest row of [`re-pins.md`](../../blob/ledger/re-pins.md) on the `ledger` branch gives the commit and date |
| Substrate version the pin runs with | `1.0.0` of the Giant Swarm line [giantswarm/substrate](https://github.com/giantswarm/substrate) — its release `v0.0.30-gs.4` (2026-09-15): upstream kagent-dev/substrate v0.0.29 (the `go/go.mod` replace since kagent-dev/kagent#2802 — the ate-api contract, and the router that addresses an actor by the `ate-target-actor` header, which this kagent sets and Substrate 0.0.27 did not know) plus the line's fourteen carried patches, among them since gs.1: `WorkerPool.spec.template` spread and anti-affinity (gs.2), a paused actor's resume failing fast when its node is gone (gs.2), every pause durable (gs.3), and the bounded golden boot (gs.4, giantswarm/substrate#39): a golden actor whose workload exits before its readiness probe answers is booted three times, then its ActorTemplate fails with `GoldenActorNotReady` and the workload's last output lines in `error_message` — which this controller's pair reconciler surfaces verbatim as `Ready=False ActorTemplateRetrying` and starts over within its golden-boot budget, `ActorTemplateFailed` once the budget is spent (giantswarm/giantswarm#37801, giantswarm/kagent-upstream#72); the worker image `ateom-gvisor:0.0.30-gs.4` is what quotes the workload's output. gs.5 (2026-09-19) is a source move with no runtime change beyond gs.4: the line publishes to gsoci from CircleCI (giantswarm/substrate#46) and its chart defaults name the gsoci copies of the Docker Hub and third-party images (giantswarm/substrate#42, #43, #45). `1.0.0` (2026-09-19, giantswarm/giantswarm#37873) is the first release of the line's decoupled scheme — its own stable semver, published by the orb's stock jobs — with the runtime of gs.5; its `atenet-router` and `atenet-egress` run the agentgateway line's `2.0.0` (`gsoci.azurecr.io/giantswarm/agentgateway-upstream/agentgateway:2.0.0`; upstream agentgateway `main` ≥ `9f9744cf`, the substrate ingress header of agentgateway#3409). The Substrate the platform's meta chart runs, whose worker `gsoci.azurecr.io/giantswarm/substrate/ateom-gvisor:1.0.0` the published chart stamps into `substrateWorkerPool.workerImage` (`SUBSTRATE_VERSION` / `SUBSTRATE_REPO` in the `Makefile`, read by `ci.yaml` through `make substrate-pin` and by the pipeline's `push-charts` job at publish; `kubectl-ate` for the e2e bootstrap from upstream's v0.0.29 release). The three lines move together, in the order agentgateway → Substrate → kagent (giantswarm/giantswarm#37742) |
| Tracking | giantswarm/giantswarm#37010 (the line), giantswarm/giantswarm#37742 (the upstream exit of every patch) |

## Which kagent are we running

Carried commits, in the order they sit on top of the pin (`git log --oneline main..giantswarm`).
Their SHAs change at every re-pin; the ledger records the SHAs of each rebase.

| Carried commit (subject) | Why the platform needs it | Upstream |
|---|---|---|
| `fix(controller): accept a RemoteMCPServer labeled kagent.dev/discovery=disabled without listing its tools` | the per-agent muster `RemoteMCPServer` carries a toolset header and must not be tool-listed by the controller, which has no bearer | kagent-dev/kagent#2752 — open |
| `fix(controller): keep a kagent.dev/discovery=disabled MCPServer out of the catalog` | the kmcp `MCPServer` half of the same opt-out | kagent-dev/kagent#2752 — open |
| `fix(helm): sanitise the chart version in the app.kubernetes.io/version label` | helm-controller installs the chart as `<version>+<digest>`, which is not a valid label value | to file |
| `fix(helm): trim every trailing non-alphanumeric from the truncated label values` | `helm.sh/chart` and `app.kubernetes.io/version` are cut to 63 characters and only a trailing `-` was trimmed; a dev build of a long branch (`0.11.0-dev.fork-repin-substrate-gs5.…`) was cut after a `.` and the apiserver refused every labelled object, so the upgrade failed and rolled back | to file |
| `fix(docker): apk upgrade in the Go runtime image` | the same Alpine finding in the controller and Go ADK images (`go/Dockerfile`), which #2756 did not cover | to file (the harness half is upstream's own pattern) |
| `ci(fork): publish dev images and charts to ghcr.io/<owner> from the poc branch` | the GitHub Actions publish the line began with (`tag.yaml`); superseded by `ci(fork): publish to gsoci from CircleCI` below, which removed the workflow — the commit stays in the stack | fork-only, never upstream |
| `ci(fork): retry image builds and keep the matrix running on a single failure` | hosted-runner flakiness of `apk.cgr.dev`; the retry lives on in the pipeline's image jobs | fork-only |
| `ci(fork): test and scan the consumed branch, re-pin weekly by workflow, keep a ledger` | this document, `ci.yaml`/`image-scan.yaml` on the consumed branch, `sync-upstream.yaml`, the ledger | fork-only |
| `ci(fork): stamp the golang-adk digest and SUBSTRATE_VERSION into the chart at publish` | the published chart carries the index digests of its own runtime images and the Substrate worker image of the Makefile's pin, so the platform's meta chart stops re-pinning by hand what the line already knows (giantswarm/giantswarm#37765) | fork-only |
| `ci(fork): publish to gsoci from CircleCI` | the org publishes to `gsoci.azurecr.io` from CircleCI only — the architect orb's multi-arch builds, cosign keyless signatures under the project's CircleCI identity, SBOM attestations — and never from a GitHub Actions job holding a registry credential. `.circleci/config.yml` ("What is published") replaces `tag.yaml`, which pushed to ghcr.io; the stamping and the ledger row moved with it | fork-only, never upstream — ours to keep |
| `feat(helm): controller.agentImage.digest and an optional default Harness for the chart's own runtime image` | the platform `Harness` of the Go ADK runtime renders from the chart's own digest (`harness.create`, off by default) instead of a digest copied into the meta chart | to file — kagent-dev/kagent#2529 is the open bug (no build-time digest), kagent-dev/kagent#2536 (closed, unmerged) proposed the key this patch uses |
| `fix(helm): drop empty-valued matchLabels from the Harness admission selector` | the platform Harness admits templates by `agent-platform.giantswarm.io/harness` alone, so the meta chart has to remove the chart's default selector key `kagent.dev/harness`; the null that deletes a key on coalesce does not survive the JSON merge patch of a kagent HelmRelease that pre-existed the cut-over (graveler, 2026-09-13: the selector kept both labels and no AgentTemplate was admitted — giantswarm/agent-platform#418). The template now drops every `matchLabels` entry whose value is the empty string, which every values path stores, and fails the render when nothing is left to select by (an empty selector would admit every template) | to file — folded into row 36's upstream branch `upstream/helm-default-harness` (the Harness template is not upstream yet; the two travel as one pull request); giantswarm/giantswarm#37742 row 41 |
| `feat(helm): a ConfigMap of the chart's runtime image references for the Substrate image cache` and `fix(helm): label the runtime images ConfigMap for helm-controller to watch` | the pinned-image set of Substrate's image cache follows the chart's digests instead of a third copy in the meta chart; the label `reconcile.fluxcd.io/watch: Enabled` (helm-controller's default `--watch-configs-label-selector`) makes the substrate HelmRelease that reads the ConfigMap through `valuesFrom` follow a Harness-image-only change at once instead of on its 10-minute interval (giantswarm/giantswarm#37767) | to file — prepared in the fork as one commit: branch `upstream/helm-runtime-images-configmap`; queued behind the Substrate `pinnedImages` flag it feeds; giantswarm/giantswarm#37742 row 37 |
| `fix(controller): honour AUTH_MODE and wire the trusted-proxy authenticator` | the platform fronts the controller with agentgateway, which validates the caller's JWT and sets the identity header from its `email` claim; the controller re-derives the identity from the bearer (`controller.auth.mode: trusted-proxy`, `controller.auth.userIdClaim: email`) instead of trusting an `X-User-Id` from whoever reaches it. On `main` the chart renders `AUTH_MODE` and nothing has read it since kagent-dev/kagent#2595 | to file — prepared in the fork: branch `upstream/trusted-proxy-auth-mode`; giantswarm/giantswarm#37742 row 2 |
| `fix(helm): keep the kagent-crds CRDs on uninstall (helm.sh/resource-policy: keep)` | the `kagent-crds` chart ships the CRDs as plain templates (no `crds/` directory), so an uninstall of that release deleted the five CRDs and every `AgentTemplate`/`RemoteMCPServer` with them; the meta chart relies on both surviving. Added as a `+kubebuilder:metadata:annotations` marker on the five v1alpha3 types, so controller-gen emits it and `make controller-manifests` stays the only producer of the templates | to file — prepared in the fork: branch `upstream/kagent-crds-resource-policy-keep`; giantswarm/giantswarm#37742 row 22 |
| `feat(api): a per-source credential for private git skill and plugin sources` | two of the platform's live agents take their skills from private repositories, and `main` has no credential path for artifact sources (the materialiser shells out to git with the process environment and no helper). `skills[].source.git.credentialRef` (and `plugins[].source.git.credentialRef`) names a same-namespace Secret key; the compiler binds it to the source's host as `Authorization: Basic <Secret value>` through the egress gateway's credential injection (upstream kagent-dev/kagent#2868), the same path a model key takes; the actor holds an inert placeholder in `KAGENT_ARTIFACT_CREDENTIAL_<hash>` and clones with no credential of its own. The Secret key holds the base64 of `<username>:<token>` (GitHub: `x-access-token:<token>`). One credential serves every source of the agent tree on that host, public ones included; two Secrets for one host are refused. Reshaped at the 2026-09-22 re-pin: before it the token was resolved into the actor's environment and offered by a git credential helper, the design giantswarm/giantswarm#37742 row 26 marked as interim. Chosen over an ambient per-Harness credential in the spike giantswarm/giantswarm#37321: a missing Secret fails only the referencing templates, and the token reaches only the actors that reference it | to file — prepared in the fork: branch `upstream/artifact-source-credential-ref`; giantswarm/giantswarm#37742 row 4 |
| `fix(a2agateway): fail a task whose dispatch to the runtime fails instead of leaving it submitted` | a turn the gateway could not deliver to the AgentInstance runtime (a failing dial, a failing ingester start, or a runtime stream that errored before its first event) left its task `TASK_STATE_SUBMITTED` for good: the instance kept it as its active task, every later message was refused as a conflict and every client saw a turn that never ends. The task is now recorded `TASK_STATE_FAILED` with the cause as its status message once the runtime says it does not hold the task, the status update reaches the caller and the instance's active task is released; the reconciler fails a submitted task the runtime does not know once it is older than the dispatch grace period (one minute) | to file — prepared in the fork: branch `upstream/a2agateway-dispatch-failure`; giantswarm/giantswarm#37742 row 33 |
| `fix(controller): start a crashed golden boot over instead of failing the AgentTemplate for good` | a golden boot that ends `GoldenActorCrashed` was terminal in the controller: Substrate marks the ActorTemplate failed once and never retries it, and the controller only carried that verdict into `Ready=False` — so a worker pod replaced under the golden actor (the WorkerPool rolling while a Harness change recompiles every template, as in the 4.7.19 → 4.8.0 upgrade) left every AgentTemplate unrunnable until something else changed the Harness (giantswarm/giantswarm#37766). Substrate reports no cause on the Actor, so an infrastructure crash and a workload that exits after it came up end in the same unattributed message (`golden actor crashed before its snapshot was taken`); the controller now starts exactly that crash over — deletes the crashed ActorTemplate with its golden actor and creates the desired one again — up to five times per revision with a doubling backoff (20 s, 40 s, 80 s, then 2 min), reports `Ready=False ActorTemplateRetrying` meanwhile and `ActorTemplateFailed` with the count once the budget is spent; a crash the resume attributed (the Substrate line's fail-fast for an unpullable image), `GoldenActorInvalid` and `GoldenActorUnexpectedState` stay final at once | to file — prepared in the fork: branch `upstream/golden-boot-retry`; giantswarm/giantswarm#37742 row 39 |
| `fix(a2agateway): the turn a unary SendMessage starts outlives the caller's deadline` | a unary A2A `SendMessage` forwarded the call to the AgentInstance runtime on the caller's context, so a caller whose gRPC deadline expired mid-turn cancelled the runtime's turn with it and the task stayed `TASK_STATE_SUBMITTED` for good — the instance refused every later message as a conflict (spidertron 2026-09-13 13:52Z: the Dev Portal's HITL answer, sent unary with its 30 s timeout, killed the turn it had just started; giantswarm/giantswarm#37771). The unary path now dispatches like the streaming one — a `taskRun` on the runtime's event stream, detached from the caller's context, persists every event and releases the instance when the task quiesces — and the caller waits on that run (the quiescent task, the runtime's message, or the first report of the task when the request asks to return immediately); a caller that leaves gets the task so far with its context's error while the run finishes the turn. The reconciler fails a submitted task beyond the dispatch grace period that the runtime still runs but no run of this gateway records (a controller restart mid-turn), cancelling it at the runtime best-effort, so the instance takes the next message | to file — prepared in the fork: branch `upstream/a2agateway-detached-dispatch` (row 33's `upstream/a2agateway-dispatch-failure` + this patch, `05f5bd17`, DCO-signed, no fork references; its message is the pull-request text). kagent-dev/kagent searched 2026-09-13: no issue or PR on a caller's deadline cancelling the turn (#2112 is the SDK client timeout knob); giantswarm/giantswarm#37742 row 42 |
| `fix(adk): load a SKILL.md whose frontmatter carries fields outside the agentskills.io set` | the Go ADK's `skill.Parse` decodes the `SKILL.md` frontmatter with `KnownFields(true)` and knows the agentskills.io fields only, so one field outside that set — Claude Code's `user-invocable`/`argument-hint`, a top-level `version:` — failed the whole agent's golden boot (`failed to load skills: invalid frontmatter … field user-invocable not found in type skill.Frontmatter`); on gazelle's cut-over (2026-09-13) `sre-agent` (four private `giantswarm/claude-code` skills) and `vera` (`loomora-marketplace-data` with `version:`) stayed unrunnable while the same skills had loaded on kagent 0.10's Python runtime, whose `Frontmatter` allows extra fields (giantswarm/giantswarm#37772). The runtime now hands the ADK a filesystem view (`go/adk/pkg/skillfs.Lenient`) in which every skill's `SKILL.md` carries only the fields the ADK knows; the ADK's parser, validation (`name`/`description` required, directory name must match) and resource rules stay the single authority, `metadata` is untouched, and a `SKILL.md` the filter cannot read as frontmatter is served unchanged so the ADK reports it as before | to file — prepared in the fork: branch `upstream/skills-lenient-frontmatter` (mirror `main` @ the pin `6a6b54ff` + the patch, `84fb5872`, DCO-signed, no fork references; its message is the pull-request text). google/adk-go searched 2026-09-13: no issue or PR on unknown frontmatter fields (`main` still `KnownFields(true)`; #1296/#1301 fixed the scalar `allowed-tools` form only), the agentskills.io reference validator rejects unknown fields while adk-python's `Frontmatter` allows them; kagent-dev/kagent has nothing on it. The ADK-side fix (decode without `KnownFields`) is a candidate for google/adk-go once the team reviews the queue; giantswarm/giantswarm#37742 row 43 |
| `feat(models): add prompt caching for the Anthropic provider` and `test(adk): deep-copy caller objects in the prompt cache no-mutation tests` | the Go ADK sent every Anthropic request without `cache_control`, so each model call of an agent loop re-processed the whole prefix — the system prompt and tool schemas (~6 k tokens for the platform's default agent), then the growing history — at full input price and with a time to first token that followed the uncached prefix (gazelle 2026-09-14: 26 of 26 `claude-sonnet-5` requests with `cache_read.input_tokens=0`; 0.7-1.5 s to the first token at 6 k input tokens, 9.7-13.5 s at 33-40 k; one twelve-call status question re-sent 340 k input tokens for $0.73 — giantswarm/giantswarm#37788). `spec.anthropic.promptCaching` (bool) and `spec.anthropic.cacheTTL` (`5m`, `1h`) on `ModelConfig`, wired through `adk.Anthropic` to both runtimes: when on, every Messages request marks the last tool definition, the last system block and the last content block of the latest turn with `cache_control`, so each call reads its previous history from the cache and writes only the new turn, and `cache_read_input_tokens` surfaces as the usage's cached count. Opt-in upstream, as Bedrock's knob is; the platform's meta chart turns it on for every Anthropic ModelConfig it renders | kagent-dev/kagent#2788 — **open** (`a0c974bd`, `f5679c2a`), carried as a cherry-pick until upstream merges it and the next re-pin drops it; giantswarm/giantswarm#37742 row 7 |
| `feat(helm): the Harness's context compaction from harness.compaction` | upstream kagent-dev/kagent#2790 (`7471879e`) puts context compaction on `Harness.spec.kagent.compaction`, not on the AgentTemplate, so the platform `Harness` the chart renders (`harness.create`) is the one place every admitted agent takes it from; `harness.compaction` (empty by default) renders under `spec.kagent` | fork-only (the chart's `Harness` template is the fork's) |
| `fix(a2agateway): end a turn whose runtime is lost, and delete a paused or crashed Actor as it is` | a session kagent parks on `input-required`/`auth-required` is paused, not suspended: its only checkpoint lives on the worker's node, and when that node goes (a spot interruption, a consolidation, a node roll) nothing notices until the person answers — the resume fails after Substrate's parking budget with the text of the last refusal, kagent's gateway records it as one more failed dispatch, every retry repeats the wait, and the session cannot be deleted because `Delete` suspends first and suspending a paused Actor dials the atelet on the node that is gone (gazelle 2026-09-15, a session paused 2026-09-14 06:06Z whose node was interrupted 12:20Z; giantswarm/giantswarm#37795). The gateway now asks the lifecycle workflow after every failed runtime stream whether the runtime is lost — the Actor `CRASHED` (what Substrate makes of an Actor it cannot bring back: a restore out of time, a worker gone, and with the Substrate line's companion patch a pause checkpoint whose node is gone) or gone — and if it is, fails the task with the cause under the message prefix `runtime lost: ` (`go/api/a2a` `RuntimeLostMessagePrefix`), moves the instance to `FAILED` with `failure.reason` `RuntimeLost` and the same message, and refuses later sends with that message without dialing the runtime, instead of asking the runtime whether it took the task (a second parking budget). The transcript stays readable; `Delete` deletes a `PAUSED` or `CRASHED` Actor as it is (`any_state` for the paused one) and suspends only a live Actor first. A runtime whose state Substrate cannot report is not presumed lost. Docs: `docs/architecture/runtime-and-lifecycle.md` "Lost runtimes" | to file — prepared in the fork: branch `upstream/runtime-lost` (`3e5b77ef`, on the mirror `main` plus `upstream/a2agateway-dispatch-failure` and `upstream/a2agateway-detached-dispatch`, which it stacks on); carried as `e821fcdc` (giantswarm/kagent-upstream#43), released in `v0.11.0-gs.17`; giantswarm/giantswarm#37742 row 45 (the Substrate line's fail-fast and crash reason are row 44; the pause TTL is a separate patch) |
| `feat(a2agateway): suspend a runtime paused for input once the pause is older than a TTL` | a turn that ends `input-required`/`auth-required` pauses the actor on its worker's node (`PauseActor`, the upstream design of kagent-dev/kagent#2714) and nothing made that checkpoint durable, so a spot interruption, a consolidation or a node roll while a person took their time lost the session's runtime state: the reply's resume found no worker on the recorded node, Substrate reported the locality miss as `no free workers available`, the task failed after the 60 s resume budget and the instance could neither be resumed, suspended nor deleted (gazelle 2026-09-14/15, giantswarm/giantswarm#37795). The controller's leader now sweeps READY instances whose latest task waits for input without a recorded snapshot and, once the pause is older than `KAGENT_PAUSED_RUNTIME_TTL` (2m; `controller.pausedRuntimeTTL`; 0 disables), suspends the actor durably — `SuspendActor` uploads the pause checkpoint to the snapshot store — and records the snapshot as the task's boundary (`RecordAgentInstanceTaskSnapshot`, guarded on the task still waiting, so a reply that landed meanwhile keeps its own boundary); the sweep takes the instance's quiesce lock without waiting and the gateway dispatches under the runtime-call lock, so a suspend and a reply never overlap. A reply within the TTL keeps the in-place resume; a later one restores from the external snapshot on any worker. The platform's meta chart leaves the default | to file — prepared as `upstream/a2agateway-pause-ttl` on the mirror; the first half of giantswarm/giantswarm#37742 row 45 (the second half is the row above: giantswarm/kagent-upstream#43, `v0.11.0-gs.17`); kagent-dev/kagent#2503 is the adjacent resume-reliability issue |
| `fix(a2agateway): leave a pause whose node has no worker alone, and delete a runtime that cannot be suspended as it is` | the pause TTL's first sweep on graveler (2026-09-15 13:46Z) tried to suspend three paused Actors whose checkpoint nodes were gone: Substrate fails the upload at once (`UploadPausedCheckpoint: while getting atelet conn for node …`) and leaves the Actor `SUSPENDING`, which the lost-runtime patch's `Delete` then suspends first — and fails the same way — so sessions that were deletable while `PAUSED` were not any more. `Idle` counts a `PAUSED` Actor only while a worker runs on its checkpoint's node (`ListWorkers`), so the sweep leaves a pause on a lost node to the node-loss handling (Substrate row 44); `Delete` falls back to an any-state delete when the pre-delete suspend fails | to file — folded into `upstream/a2agateway-pause-ttl` (giantswarm/giantswarm#37742 row 45) |
| `feat(runtime): name the agent and the user on every model call` | under the kagent API v2 every agent is a Substrate actor in a shared WorkerPool and every actor connection leaves the worker through the Substrate egress, so a gateway in front of the model providers (the platform's `llm` listener, which prices every call and labels usage per agent and user) sees one caller for every agent and no user: since the platform's cut-over every dollar of agent spend sat on the egress row and the `user` label was unknown (giantswarm/giantswarm#37871). The controller now injects `KAGENT_AGENT_TEMPLATE` next to `KAGENT_NAMESPACE`; the Go ADK sets `x-kagent-agent` and `x-kagent-agent-namespace` on every model call after a ModelConfig's default headers (so none can name another agent) and `x-kagent-user`, the person of the turn — the identity the controller's trusted-proxy authenticator resolved from the caller's validated token, which the a2a gateway forwards to the actor as metadata of the same name; a session without claims (unsecure mode and its `admin@kagent.dev` default, an agent's propagated caller id) forwards nothing, so the header is absent, never a token or a stand-in; the Claude harness sends the two agent headers through `ANTHROPIC_CUSTOM_HEADERS`, which the controller renders and owns. The gateway reads the headers only from the egress's traffic (giantswarm/agent-platform#586) | to file — prepared in the fork: branch `upstream/model-call-identity-headers`; giantswarm/giantswarm#37742 row 48 |
| `fix(controller): compare the pair collections' outputs by content` | the PairReconciliation and PairRuntimeObservation collections carry the desired and observed ActorTemplates, protobuf messages, and left their comparison to krt's default, reflect.DeepEqual: a message initialises its reflection state on its first protobuf operation, the reconciler performs those on the graph's messages from its own goroutine, so the graph's reflect.DeepEqual raced with them — the race detector failed the controller tests' CI twice (2026-09-15, 2026-09-18) — and told equal messages apart by that state; both types implement krt's Equaler now, the messages compared with proto.Equal | to file — prepared in the fork: branch `upstream/pair-collection-equality`; giantswarm/giantswarm#37742 row 49 |
| `ci(fork): publish through the orb's stock jobs with decoupled versions` | the RFCs (giantswarm/rfc `semantic-versioning-of-upstream-software`, `semver-based-automatic-upgrades`) give a package of upstream software its own stable semver, `-rc.N` and gitsemver's dev shape, nothing else: the images are built by the orb's `build-image` (native per architecture, the Makefile's build arguments through `build-args`) and `push-to-registries` (merge, sign, attest, the upstream-pin annotation), the version is gitsemver's (`vX.Y.Z` tags → `X.Y.Z`, first `1.0.0`), the pipeline's own `version` and image jobs and the `-gs.N` scheme are gone, pull request branches build without pushing, and `mirror.sh` refuses an upstream tag the fork already holds at another commit ("What is published", "Release scheme") | fork-only, never upstream — ours to keep |
| `fix(ui): the chat journeys no longer race two tooltips and the reply` | the fork's `ui-tests` runs upstream's browser suite on GitHub-hosted runners, slower than upstream's, and on every run one to three chat journeys timed out on Firefox — three consecutive attempts failed a pull request that touched nothing under `ui/` (giantswarm/kagent-upstream#68). Two of the three were the product: the rail row's ellipsis tooltip opened to the right of the name, over the row's own menu button, and a pointer moving from the name to the menu landed on the tooltip, which kept it open with the button unreachable under it for the rest of the test (both engines, every run, reproduced locally with the trace); the snapshot mark's Delete carried a tooltip that its own comment forbids, beside the confirmation the pointer opens. The third was the spec: the composer disables the snapshot button while the reply streams, and the checkpoints spec asserted it enabled from the moment the reader's message showed, giving a streamed reply the assertion's five seconds (the turn takes five on a mildly loaded run). The specs wait on the turn's end (`sendAndAwaitTurn`; the transcript spec asserts the assembled reply after the turn, as its second turn already did), press the rail's Duplicate once it has stopped moving, the rail tooltip opens above the row, the Delete tooltip is gone, and the twelve-step questions journey is two journeys with their own budgets and one page load fewer (a boot of the dev server is ten seconds and more on a busy runner) | to file — prepared in the fork: branch `upstream/ui-chat-journeys` (mirror `main` + the patch, DCO-signed, no fork references; its message is the pull-request text). kagent-dev/kagent searched 2026-09-23: #2773 (two specs flaking under parallel load) and #2871/#2877 (the Cancel label two confirmations shared) are the neighbours, nothing on the tooltips or the snapshot button's wait; upstream's own `ui-tests` on Blacksmith runners reports no flaky test. giantswarm/giantswarm#37742 row 51 |
| `ci(fork): keep the browser suite's evidence of a failed run` | `ui-tests` uploads `ui/test-results` as a seven-day artifact when the job fails: the screenshot and video of every failed attempt, the trace of every retry. The log names the step that timed out; only the trace shows what was on the page and under the pointer while it did, and the hosted runner discarded it with the workspace (giantswarm/kagent-upstream#68 had to be diagnosed from a local reproduction for that reason) | fork-only |
| `fix(controller): start a golden boot that failed its boot bound over` | the Substrate line's boot bound (giantswarm/substrate#39) fails an ActorTemplate with `GoldenActorNotReady` once its workload missed the readiness probe on three boots in a row, about a minute apart, and the controller carried that into `ActorTemplateFailed` for good: a new AgentTemplate with three git skills that booted while the platform's egress restarted failed every skill fetch (`Recv failure: Connection reset by peer`), stayed failed long after the egress was back, and was Ready 5 s after a person deleted it for Flux to recreate (giantswarm/kagent-upstream#72). The controller now starts a `GoldenActorNotReady` boot over like the unattributed crash above — the same backoff (20 s, 40 s, 80 s, then 2 min) and the same budget of five retries per revision, which both failures spend; the recreated template brings a fresh bound. `Ready=False ActorTemplateRetrying` quotes Substrate's message, the workload's output included, from the first failure on; the final `ActorTemplateFailed` leads with the count, `golden boot 6 of 6 failed (…); no retries left` | to file — the second commit of the prepared branch `upstream/golden-boot-retry`, filed with or after Substrate's boot bound upstream (giantswarm/giantswarm#37742 rows 39 and 47) |
| `test(ui): count a journey's steps per test, and budget the transcript's` | after the chat journeys fix the fork's `ui-tests` still failed on every attempt of a loaded Firefox run, on pull requests that touched nothing under `ui/` (giantswarm/kagent-upstream#68): the transcript journey is fifteen steps around two page loads, three streamed turns, a reload and a conversation switch on the thirty-second default, 31–34 s on the hosted runner with every step waiting on the page. The suite's own rule gives a test of ten or more steps `LIFECYCLE_TIMEOUT`, but its check counted steps per file and only in files holding one test; it counts per test now, finds this journey alone, and the journey sets the budget in its own body. Profiled first: a turn's time is React's development build re-rendering the page per streamed word, no race in the product | to file — prepared in the fork: branch `upstream/ui-journey-budget` (mirror `main` + both `test(ui)` commits, DCO-signed, no fork references). kagent-dev/kagent searched 2026-09-24: nothing on the transcript journey, the step check or the templates delete; #2610/#2612 (earlier waits) are the neighbours. giantswarm/giantswarm#37742 row 57 |
| `test(ui): press a template's own Delete, once its page is there` | the agent-templates journey's step 13 was reported flaky on most `ui-tests` runs of both engines: the router renders a new route in a transition, so the address matches while the list is still on screen, and the list row's unlabelled delete carries the page's test id — the press opened the row's confirmation, which left with the list; on Firefox the result link was pressed while the searched list re-rendered, took the focus and navigated nowhere. The step waits for the one row and for the page's labelled Delete, as steps 2 and 11 already did | to file — same branch and row as the line above |

Rules for the table: every non-`ci(fork)` row has an upstream pull request or a "to file" that
giantswarm/giantswarm#37742 tracks; a row leaves when the sync drops the commit because upstream merged it
(the re-pin report names the upstream commit) — remove the row in a follow-up pull request. Giant Swarm-specific
wiring (Substrate settings, the platform `Harness` objects, pgvector, registries) lives in the agent-platform
charts, not here.

## What is published

`.circleci/config.yml` (the fork's own, hand-written — upstream has no CircleCI pipeline) publishes on every push to the
consumed branch `giantswarm` and on the line's release tags `vX.Y.Z` and `vX.Y.Z-rc.N`, through the architect orb's
stock jobs: one `build-image` job per image and architecture (amd64 and arm64, each native on a machine of its
architecture, pushed by digest) and one `push-to-registries` job per image, which joins the two into the tagged index,
signs and attests it. Every other branch of the line — a pull request branch, a `sync/**` re-pin candidate — runs the
same eight builds with `push: false` and publishes nothing; the mirrored upstream refs (`main`, `release/v*.x`,
upstream's tags) run nothing:

| Artifact | Reference |
|---|---|
| Images (multi-arch, amd64 + arm64) | `gsoci.azurecr.io/giantswarm/kagent/{controller,ui,golang-adk,claude-harness}:<version>` — upstream's Dockerfiles as they are (`go/Dockerfile` with `BUILD_PACKAGE` for the controller and the Go ADK, `ui/Dockerfile`, `go/harness/claude/Dockerfile`), built with the build arguments the Makefile passes (`VERSION`, the `LDFLAGS` `-X` flags for the version and the commit, `BUILD_PACKAGE`), signed with cosign keyless under the project's CircleCI OIDC identity (`cosign verify --certificate-oidc-issuer https://oidc.circleci.com --certificate-identity-regexp '^https://circleci\.com/api/v2/projects/[a-f0-9-]+/pipeline-definitions/[a-f0-9-]+$'`), the provenance and the SPDX SBOM attestation of each platform signed the same way; the index carries the annotation `io.giantswarm.upstream.version=main@<pin sha7>`, the upstream pin ("Re-pin") |
| Charts | `oci://gsoci.azurecr.io/giantswarm/kagent/helm/kagent:<version>`, `oci://gsoci.azurecr.io/giantswarm/kagent/helm/kagent-crds:<version>` — the line's own path, not `charts/giantswarm/kagent` (the 0.10 wrapper chart's, below); signed like the images. The chart's image defaults are stamped with the same registry, repositories and version, `controller.agentImage.digest` and `runtimeImages.claudeHarness.digest` with the index digests of that build's golang-adk and claude-harness images, `substrateWorkerPool.workerImage` with the worker of the Makefile's `SUBSTRATE_VERSION` |
| Dev version of a push to `giantswarm` | gitsemver's dev shape as the orb's gitsemver emits it today, `X.Y.Z-dev.giantswarm.<YYYY-MM-DD>.<HH-MM-SS>.h<sha7>` — the RFC's successor shape `X.Y.Z-r<crc32 of the branch>t<YYYYMMDDHHMMSS>h<sha7>` arrives with the orb's gitsemver, and the filter below moves with it. `X.Y.Z` is the next patch above the newest stable tag reachable from the commit (upstream's mirrored `v0.9.10` until the line's `v1.0.0` is on the branch, then `1.0.1`; a re-pin rebases the branch past the line's tags, so the base falls back to upstream's until the next release tag), then the branch, the committer date in UTC, the short commit. Consumers select the channel with a Flux `semverFilter` of `.*-dev\.giantswarm\..*` and no range (a range never admits a pre-release). Flux takes the highest match, and the base outranks the time stamp: after a re-pin the channel stays on the last build before it (`1.0.1-dev.…` above `0.9.11-dev.…`) until the release that follows the re-pin is tagged — a re-pin is released, not left to the filter |
| Release version of a tag | the tag without `v`: `X.Y.Z`, or `X.Y.Z-rc.N` for a release candidate (the release scheme below; the workflow's tag filter admits no other shape) |
| Digests | every build's image and chart digests (multi-arch index digests, what a `Harness` pins) are a row of [`builds.md`](../../blob/ledger/builds.md) on the `ledger` branch and the `published.md` artifact of the pipeline's `record` job |

Only what the platform consumes is published: the Python ADK, the Codex harness and the CLI image are not
offered on the platform and are not built here. A dev build is the test artifact of the consumed branch — a merged
pull request or a landed re-pin — proven in agentlab on the meta chart's dev channel before the tag that consumers
depend on; a pull request's branch is proven with a build of its checkout swapped into the lab (the pipeline builds
it, pushes nothing). Its consumers: the agent-platform meta chart's dev line (`components.kagent` follows the channel;
its `kagent.tag` and the two `Harness` image digests are pinned there and re-pinned per build), agentlab, and the
test cluster following the channel. A release (below) is what the meta chart's release line resolves.

Publishing does not wait for the tests. Nothing reaches the consumed branch untested — a pull request needs a
green `ci-ok` and `scan-ok` on an up-to-date branch, and the sync lands a candidate only after both passed on
its exact head (the same commits it then force-pushes) — so the push-triggered run on the consumed branch is a
repetition; a red one is a flake to re-run or an environment drift to fix, visible on the commit and in the
Actions tab, never a reason to pull a build that was green on the same tree.

### Release scheme

A release of the line is a tag `vX.Y.Z` on the consumed branch — its own stable semver, decoupled from upstream's
version, as the RFCs prescribe for every Giant Swarm package of upstream software (giantswarm/rfc
`semantic-versioning-of-upstream-software`, `semver-based-automatic-upgrades`): the version says nothing about the
upstream version, which is documented instead — the pin in the table above and the ledger, the annotation
`io.giantswarm.upstream.version` on every image, the release's row below. `1.0.0` is the first release of the scheme,
the next major above the coupled `0.11.0-gs.N` releases it replaces, so every consumer range moves forward and none
back; a patch or a minor of the line is a patch or a minor of the version, whatever upstream did in the re-pin between;
a major is the line's own breaking change (a chart value renamed, a CRD contract changed), never upstream's numbering.
A release candidate is `vX.Y.Z-rc.N`. The pipeline publishes a tag the way it publishes a dev build — the version is
the tag without `v`; the workflow's tag filter admits `vX.Y.Z` and `vX.Y.Z-rc.N` and nothing else: the four images and
both charts under `gsoci.azurecr.io/giantswarm/kagent` at `X.Y.Z`, the digests as a row of `builds.md`. The release's
row in the table below is written by hand.

Why this shape:

- **Order.** Releases sort as plain semver, so a consumer follows the line with a range — the meta chart's release
  line `>=1.0.0 <2.0.0` — and re-resolves on every reconcile. Dev builds (`-dev.…`) and release candidates (`-rc.N`) are
  pre-releases, which Flux's semver (Masterminds) never matches against a range without a pre-release bound; the dev
  channel is selected by `semverFilter` (above) and nothing else.
- **No collision with the mirror.** Upstream's tags are `vX.Y.Z` too, and the mirror copies every upstream tag the
  fork lacks; a name both use would make the two refs disagree. The line's tags therefore live in a major above
  upstream's — `1.x` against upstream's `0.x` — and `scripts/fork/mirror.sh` refuses, naming the tag, to mirror an
  upstream tag whose name the fork already holds at another commit, instead of skipping it silently. The rule: when
  upstream's version approaches the line's major (upstream tags `0.99.x`, or a `1.0.0-rc`), the line moves to its next
  major first — a deliberate `v2.0.0` — and the consumers' ranges with it. Upstream's version resolvers in `ci.yaml`
  see the fork's tags too: `scripts/upgrade-from-version.sh` (the `adjacent` leg) is skipped on a non-release base such
  as `giantswarm`, and `scripts/prev-stable-version.sh` only looks at the release line below the one being built
  (`release/v0.10.x` today), where no fork tag lives.
- **Out of the wrapper's range.** Every 3.x installation of the platform selects the 0.10 wrapper chart `kagent` at
  `oci://gsoci.azurecr.io/charts/giantswarm/kagent` with `>=0.2.0 <1.0.0` and re-resolves it on every reconcile.
  The line's chart must never be resolvable there. It is not: it lives on another OCI path
  (`gsoci.azurecr.io/giantswarm/kagent/helm`) — the wrapper's repository and the line's never share a version — and
  its versions are `>=1.0.0`, outside that range besides.
- **gsoci, from CircleCI, under the line's own path.** The org publishes to `gsoci.azurecr.io` from CircleCI only —
  the architect orb's multi-arch builds, signed with cosign keyless under the project's CircleCI identity — and
  never from a GitHub Actions job: no Actions workflow of this repository holds a registry credential, and nothing
  pushes to ghcr.io. `.circleci/config.yml` is that pipeline. The repositories keep upstream's names under the
  line's prefix (`giantswarm/kagent/<image>`, `giantswarm/kagent/helm/<chart>`) — not `charts/giantswarm/kagent`,
  the wrapper chart's repository (above), and not the app catalog, which lists apps, not the component a meta chart
  pins. A consumer names the registry (`kagent.registry: gsoci.azurecr.io`) and the chart repository
  (`oci://gsoci.azurecr.io/giantswarm/kagent/helm`); the version scheme is the same for both.

A tag is cut only after the build of the same commit — its dev build on the consumed branch — has passed the
platform's proofs in agentlab on the meta chart's dev channel; the tag re-publishes the same tree under the release
version. Cut it deliberately (no tag ruleset gates it; anyone with write access can push a tag):

```bash
git tag -a v1.0.0 -m v1.0.0 <sha> && git push origin v1.0.0
# or, without a checkout:
gh api -X POST repos/giantswarm/kagent-upstream/git/refs -f ref=refs/tags/v1.0.0 -f sha=<sha>
```

Then add the row below from the run summary (or `builds.md`).

### Releases

Rows up to `0.11.0-gs.22` are releases of the coupled scheme that preceded the one above — `vX.Y.Z-gs.N`, `X.Y.Z` the
upstream version the pin anticipated, published by the pipeline's own jobs; `v0.11.0-gs.22` the first from CircleCI.
`1.0.0` and later follow the release scheme above.
| Release | Tag | Commit | Upstream pin | Images (index digests: controller, ui, golang-adk, claude-harness) | Charts (kagent, kagent-crds) |
|---|---|---|---|---|---|
| `0.11.0-gs.1` | `v0.11.0-gs.1` (2026-09-10T23:36Z, after the agentlab proof of the dev build `0.11.0-dev.giantswarm.2026-09-10.22-06-46.h0ac5240`: platform, agents and toolsets proofs green) | `0ac5240` | `4a91c27` (2026-09-10) | `sha256:04106af50ee8e68e0388006981e25bd61b5220f50ece8fe7920269aea3b8d49c`, `sha256:3275c7ab8f09291f4806ed49e406d98c1dbec51e5d05415ed07ade2799423f81`, `sha256:969af5f733c8e2bd7756f40766352f5af744964e969a031ba146198d8becd546`, `sha256:c8a9c7c3d5dd7ecc3953fff2a12b6b4a44b46eb6a66687a5604f6851fe453ca3` | `sha256:3c9ee22cb60c493b7e99bbc27d260cb5aa5e48d0996db1bbd28abae3b17a8a9e`, `sha256:7c62f76e5693bad9b603f4e557b82da9b54060e3d1f811133905b4d6cca76d70` ([run 34542852237](https://github.com/giantswarm/kagent-upstream/actions/runs/34542852237)) |
| `0.11.0-gs.2` | `v0.11.0-gs.2` (2026-09-11T04:55Z, after the agentlab criterion-5 proof of the dev build `0.11.0-dev.giantswarm.2026-09-11.00-32-34.h3bdff38`: reached directly through a port-forward to `kagent-controller` — bypassing agentgateway — `SystemService/GetCurrentUser` returned the admin bearer's `email` while a forged `x-user-id` was ignored, a bearer-less call was refused (`Unauthenticated`), `/health` stayed `200`, and the five CRDs carried `helm.sh/resource-policy: keep`) | `3bdff381` | `4a91c27` (2026-09-10) | `sha256:9f0acec1ce78e7c9b261c199dd73be24ec404e389138a4ed909bf2e5527b8729`, `sha256:de6d8a593a49e013799a38a84d43586f2ebb84905d84b826783a343dddf88e82`, `sha256:7db42765cc401f4e356f876cf76a25de39c5109e56fabc9fe45ec0bf7e2d3137`, `sha256:82fce79679ac39a386a393aa0fbba7a2f55652bd1bc184adc5d5eae72c040c82` | `sha256:80026ab9dfbe9d82cbd2f856890cf389c58dc92c35c141af25e0a5d3e77b0c87`, `sha256:00b47fba8ae09e4e62cc5c4d6c6a40c2bb5cc596ab005e6b3d929fd6a4ec1b8c` ([run 34563969164](https://github.com/giantswarm/kagent-upstream/actions/runs/34563969164)) |
| `0.11.0-gs.3` | `v0.11.0-gs.3` (2026-09-11T07:26Z, after the agentlab proof of the dev build `0.11.0-dev.giantswarm.2026-09-11.06-51-42.hed07617` — the second automated re-pin's head, upstream `6a6b54ff`: `platform-test`, `agents-test`, `toolsets-test --skip-portal` and `skills-test` green on an isolated lab running the build as charts, images and both platform Harness digests) | `ed07617b` | `6a6b54ff` (2026-09-10) | `sha256:eeac144a79ad1c183208eeeb699a362ccc7dffd70f1032df8f5b57856dba85ac`, `sha256:77154fd68b88df89037df0c83dc26a98f1f10db5024ea8d5ca8d29cfa9e55133`, `sha256:a2d23f5eb9c01e1903459a6e742f7d4aaa5e950d7e9aa6f07f8982761be0163a`, `sha256:d8df16dc5246130ccfb1cd0ef5b6665f354d02812d2266176a3d230096556617` | `sha256:50451bb05a4706ea90503c28c574e82f64fefca5cb45ec749bf0f3e639fbd0b3`, `sha256:d7150eb061713bd42ddfd7506ebd9db7fd39c2e80b68d2bdd41100536cbb6a72` ([run 34574409389](https://github.com/giantswarm/kagent-upstream/actions/runs/34574409389)) |
| `0.11.0-gs.4` | `v0.11.0-gs.4` (2026-09-12T10:22Z, on the rebase-merged giantswarm/kagent-upstream#15 — the A2A gateway fails a task whose dispatch to the runtime fails — cut for the agentlab 4.x audit's re-pin of the meta chart, giantswarm/giantswarm#37763; the pin and the other carried patches are those of `gs.3`) | `ca399a3a` | `6a6b54ff` (2026-09-10) | `sha256:fc3eb5169a66276c467f23bf28bf932e6c7f8f92c7b56548c1f7fb38cc5868a1`, `sha256:93627659768dc3d447672807074e0d53993efb992c3cb58c85d05fb9b6ad57e3`, `sha256:6e510b241e2d000b7d10879c9fde3dd34a37879854278f0a5d386426049a88bf`, `sha256:475741e15dbe4c303a44bb80d672c05511dc3eb4118acf32b0f9ab3124a235dc` | `sha256:f5f037613d70fb982c23b4342f2d3760d828ebcc1b81e6d7a6c09944e142ca23`, `sha256:74a093857d183d5ff67fe81f3787df0699929643b7a9fbf291ae3955c548e183` ([run 34688265253](https://github.com/giantswarm/kagent-upstream/actions/runs/34688265253)) |
| `0.11.0-gs.5` | `v0.11.0-gs.5` (2026-09-12T13:26Z, on the squash-merged giantswarm/kagent-upstream#18 — Substrate re-pinned from the line's bootstrap dev build to its release `0.0.27-gs.6` — above the three commits of giantswarm/kagent-upstream#17: the publish-time stamps of the golang-adk digest and `SUBSTRATE_VERSION` into the chart (`a26a9cbf`, fork-only), `controller.agentImage.digest` with the optional default Harness (`6e52217d`) and the ConfigMap of runtime image references for the Substrate image cache (`25b4fd68`); cut for the agentlab 4.x audit, giantswarm/giantswarm#37765. The first release whose published chart carries `controller.agentImage.digest` = its golang-adk digest, `runtimeImages.claudeHarness.digest` and `substrateWorkerPool.workerImage: ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.27-gs.6`; `harness.create` stays `false`) | `955ac543` | `6a6b54ff` (2026-09-10) | `sha256:df2babf1c143ff91e5a89bcbe12a865f5f4df8e6dcb242a63ab5f9b9c183b35e`, `sha256:a56386045de6bd26bce3249af22ef29067a3a79001801f48a48aab1cd72f150e`, `sha256:f9a4367296a8753795e5442b18cfcc6323289aca3988285a5a2f5f9e1222f46a`, `sha256:9039fd94e130589bdef1d531dd17f930061381c7e44bfad775c0c71cd613855a` | `sha256:58a381906624c57c4c70ca0a7d8904fe171ef8c82146e3d39e49463119a4f840`, `sha256:1e766c92446318d2fc29c4f571e0089fef93adc2cf812104eabfde9f0bd5e3d2` ([run 34696441320](https://github.com/giantswarm/kagent-upstream/actions/runs/34696441320)) |
| `0.11.0-gs.6` | `v0.11.0-gs.6` (2026-09-12T15:07Z, on the squash-merged giantswarm/kagent-upstream#20 — Substrate re-pinned from `0.0.27-gs.6` to the line's release `0.0.27-gs.7`, which carries giantswarm/substrate#25: ateom re-asserts its capacity report every 10 s, so the worker record the pod-IP repair re-creates receives its capacity within one interval (giantswarm/giantswarm#37762); no other change since gs.5. The published chart stamps `substrateWorkerPool.workerImage: ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.27-gs.7`; `harness.create` stays `false`. Inert under the agent-platform 4.7.x meta chart, which forwards the worker image by hand; live from 4.8.0) | `1724f815` | `6a6b54ff` (2026-09-10) | `sha256:936bc3f31b5d30fca52a2e669b5742062e2b4bb9056c7fcd74b0ac6b90cc3064`, `sha256:4074b2c38d4ad4fd6907009ce774daf71906c6f347f34be73b6a512005f51e05`, `sha256:38c8c45a318e74af117e0811affd02bed1f97484a2ce05cef095334576147da1`, `sha256:47dff2ac4865ee296e926bc7f3e326f32f012be18ef98bba6f600d5e41711bde` | `sha256:90446d93b1d81991e496325fc471816a68dea8f365cb00fab3ebb5c3cf72386e`, `sha256:cdc8eefb63c6a60e8dbc906f95f0e30edfa07405175003b194685fd51d88fed3` ([run 34701286542](https://github.com/giantswarm/kagent-upstream/actions/runs/34701286542)) |
| `0.11.0-gs.7` | `v0.11.0-gs.7` (2026-09-12T18:48Z, on the rebase-merged giantswarm/kagent-upstream#22 — the `kagent-images` ConfigMap carries `reconcile.fluxcd.io/watch: Enabled`, so the substrate HelmRelease that reads it through `valuesFrom` follows a Harness-image-only change at once instead of on its 10-minute interval (giantswarm/giantswarm#37767); no other change since gs.6. Rolled into agentlab (agent-platform 4.8.0) at 18:58:31Z: the relabelled ConfigMap's new digests reached the substrate release in the same second, no `requestedAt` nudge; the Harness-swap proof is recorded on the issue) | `2b1cef30` | `6a6b54ff` (2026-09-10) | `sha256:98c43507ce8f21d6bbdda9d845b380cdc136ed727fe226ba24242c5fe2ad464f`, `sha256:d385c428d2121c782633b7e34d428e8d8c5c5c120258994c46f2f38933346571`, `sha256:cfd3ace31ffce7fe45bd62043eaccfa17029d983c734cca5d1f1f2322fa49e90`, `sha256:38fa0e5a4e2cc517468c3c2d1e6c322bb80eae4f36a98cd3f21836dbb8a964e0` | `sha256:73d933fac20717e12c826c39b5972a8a1451115cb73dc6220322d80cfbe1aff2`, `sha256:ef0294d07f70f722af8be35ceaaa66671e7ac04d8eddb339fd87d660431df903` ([run 34712322934](https://github.com/giantswarm/kagent-upstream/actions/runs/34712322934)) |
| `0.11.0-gs.8` | `v0.11.0-gs.8` (2026-09-12T19:15Z, on the rebase-merged giantswarm/kagent-upstream#23 — the controller starts a crashed golden boot over instead of failing the AgentTemplate for good (`251e23bb`; giantswarm/giantswarm#37766): Substrate's unattributed `GoldenActorCrashed` is retried up to five times per revision with a doubling backoff, `Ready=False ActorTemplateRetrying` meanwhile; a crash the resume attributed, `GoldenActorInvalid` and `GoldenActorUnexpectedState` stay final — cut after the agentlab proof of a controller build of the same patch on 4.8.0 (a Harness digest change with the worker pods replaced under the boots: both templates crashed, were started over and were Ready 72 s after the trigger with no Harness change; `platform-test` 9/9, `agents-test` 6/6); the pin and the other carried patches are those of `gs.7`) | `6be4b095` | `6a6b54ff` (2026-09-10) | `sha256:3777ccfa232c8c23ef9f012ba67a9693c0ef82f436e9e5415704a54c4b75c9f1`, `sha256:2f252ef63f3d01b52a02f27b324f0583beb7256d873a0118d6b77b3f61ad24dc`, `sha256:25cd94da2ed6424982fc7113ce95fe613210fcd5e278a071ad7b18d9a00bdc87`, `sha256:f0601bda7374edf8d72384c4996d2536f8ea9e9b70419cbd62ac940b66105129` | `sha256:3aee028d939b5b8b4bfcff002b5c4583947b70c7fc2fe5cb3fa8e662399ec991`, `sha256:ed87a03aff899ad14f9e785f4b768dc2bf0a10c409ce1be70e2ac66f5d13105c` ([run 34713645853](https://github.com/giantswarm/kagent-upstream/actions/runs/34713645853)) |
| `0.11.0-gs.9` | `v0.11.0-gs.9` (2026-09-13T12:40Z, on the rebase-merged giantswarm/kagent-upstream#26 — the Harness template drops every `allowedAgentTemplates.selector.matchLabels` entry whose value is the empty string and fails the render when nothing is left to select by (`c5fc92eb`; giantswarm/agent-platform#418): the meta chart forwards `kagent.dev/harness: ""` instead of a null, which the JSON merge patch of a kagent HelmRelease that pre-existed the cut-over removed instead of storing, so the platform Harness on graveler required both labels and admitted no template — cut after the agentlab proof of the PR branch's dev build `0.11.0-dev.fix-harness-selector-drops-empty-values.2026-09-13.12-17-29.hcc74916` on the meta chart change (giantswarm/agent-platform#428): the key removed from the live HelmRelease as on graveler, the Harness selecting by both labels and a platform-label-only AgentTemplate admitted by none; after the upgrade the HelmRelease patched to `""`, the Harness selecting by `agent-platform.giantswarm.io/harness` alone, the template `Accepted=True` → `Ready=True` (golden snapshot ready), `platform-test` 9/9, the `agents-test` agent Ready after 20 s; the pin and the other carried patches are those of `gs.8`) | `549907d2` | `6a6b54ff` (2026-09-10) | `sha256:bc678abe8e8ec4158c48c60fd01585c7f97319c3bc2b0806d2314f427d5d31c8`, `sha256:e4a93276664bd0aad495b892916567ea0348478fe510883fcad7cd4f74438cbe`, `sha256:4511c2cd495bc808c15a63d70a621bcfa432057e26c6da452fe64c3139e4146c`, `sha256:0502e5ed94bbf44f2d48eee00a1593bbbd1ac8b009c092200580023402e08c64` | `sha256:93833d6cdfdb08a1cc1c5b31b1c41cb7da636cd0d86fe3b1707dc7a5bb5982f8`, `sha256:1ee839ddb422df4c5854ca18656357065873a4ca45a93847e97f859605160aef` ([run 34757744840](https://github.com/giantswarm/kagent-upstream/actions/runs/34757744840)) |
| `0.11.0-gs.10` | `v0.11.0-gs.10` (2026-09-13T15:07Z, on the rebase-merged giantswarm/kagent-upstream#28 — the turn a unary A2A `SendMessage` starts outlives the caller's deadline (`52832849`; giantswarm/giantswarm#37771): the unary path dispatches like the streaming one, a `taskRun` on the runtime's event stream detached from the caller's context, and the caller waits on that run — a caller whose deadline expires gets `DeadlineExceeded` while the run finishes the turn and `GetTask` shows it landing; the reconciler fails a submitted task beyond the dispatch grace period that the runtime still runs but no run of this gateway records — cut after the agentlab proof of a controller build of the same tree (`kagent-controller:dev-9c5522a3`, the PR head before the rebase, via `platform.devImages.kagent` on agent-platform 4.9.1): `grpcurl -max-time 5 … SendMessage` → `DeadlineExceeded` at 4992 ms, `GetTask` `WORKING` at once and `COMPLETED` with the 654-word artifact two minutes later, the gs.9-era instance stuck on a `SUBMITTED` task accepted its next message; `platform-test` and `agents-test` green; on gs.9 the same call left the task `SUBMITTED` and the next message `FailedPrecondition`; the pin and the other carried patches are those of `gs.9`) | `52832849` | `6a6b54ff` (2026-09-10) | `sha256:147debeebbf97a57c0a8e3dfcf4704acbf7a3aa57e06ac637ba1ce0e8e130490`, `sha256:260cd6cea31b5b158c6759199310a44a164c358bff48086a2b5658c2349fdde6`, `sha256:006ca89501db3cd375e65f78f0e817cba9e1245093076b3c0d2e9696e05e236c`, `sha256:421b1e631c3e61272bc3c02e2eb05ec117a6768d79d6421a14147f4b892f5cc0` | `sha256:504e1d4e36ed3bf15618f5acf4a887ecd6b48a4cade07f90d8f18927b85433b4`, `sha256:5ea3a2ab4727ffefd6ff1ded59498305fb002c0bb6aaed8dd1b27f964f1ee50d` ([run 34764654053](https://github.com/giantswarm/kagent-upstream/actions/runs/34764654053)) |
| `0.11.0-gs.11` | `v0.11.0-gs.11` (2026-09-13T20:54Z, on the rebase-merged giantswarm/kagent-upstream#30 — the Go ADK runtime loads a `SKILL.md` whose frontmatter carries fields outside the agentskills.io set (`bd417a04`; giantswarm/giantswarm#37772): `go/adk/pkg/skillfs.Lenient` serves the ADK a view of the skills directory with only the known fields, so Claude Code's `user-invocable`/`argument-hint` and a top-level `version:` no longer fail the golden boot while `name`/`description` stay validated — cut after the agentlab proof of the PR branch's dev build `0.11.0-dev.fix-skills-lenient-frontmatter.2026-09-13.19-33-23.h23dc352` on agent-platform 4.10.1: `skills-test` with the fixture `fixtures/skills/frontmatter-fields` of giantswarm/agentlab (`user-invocable`, `argument-hint`, `version`) reached Ready on the Harness in 12 s and the turn answered the fixture's codeword; the pin and the other carried patches are those of `gs.10`) | `aca521b0` | `6a6b54ff` (2026-09-10) | `sha256:12444b6a62c31ce6c755d78e68b9b1f787c71862c191add4947f9a12924e090b`, `sha256:4015dfd54a36ee73e7b2cec5a168b19d39aa54420c9822e48790ed092486987b`, `sha256:e7c347cadce8446dc9d21c5cf32db3a4f10b5add8cf4681353ec50694779c6da`, `sha256:539b9e1af7cfda8d15e34f6f932b8fb8d43945040d9ed3fa9b98d410bff9c631` | `sha256:b79141b3466de88e8cf4998b37ddb8af7117d7f5a7aef8ef25131c17538f5ece`, `sha256:e4ba65f3f3e623ef86466b1a1006c7f05af1ad673a1898ef5c732e18ed5fc7d4` ([run 34781813093](https://github.com/giantswarm/kagent-upstream/actions/runs/34781813093)) |
| `0.11.0-gs.12` | `v0.11.0-gs.12` (2026-09-13T21:42Z, on the squash-merged giantswarm/kagent-upstream#32 — Substrate re-pinned from `0.0.27-gs.7` to the line's release `0.0.27-gs.9`, which carries giantswarm/substrate#30: the gVisor worker's terminate lists the runsc containers it knows and checks and deletes only those, and a `runsc delete` that fails after removing its container counts as done, so a superseded `AgentTemplate` revision whose golden boot had crashed is collected in one sweep instead of pinning its worker for good (giantswarm/giantswarm#37773; on gazelle two of four workers were held); no other change since gs.11. The published chart stamps `substrateWorkerPool.workerImage: ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.27-gs.9`. Cut after the agentlab proof of the Substrate patch's dev build (never collected in 56 min on the gs.7 worker; collected on the sweep after the template change with the patch); live on gazelle 2026-09-13 21:50Z, the WorkerPool rolled, the two pinned workers `FREE`, the controller log clean) | `b5bfa7f1` | `6a6b54ff` (2026-09-10) | `sha256:cdb768e7de6ebb07ffeab295f88ca3b6f2d8760bc4982a4ff5ddcc0fae9064ae`, `sha256:e540e72abab945a5c5c6ed3f751ec5bd0ef2d43bbdabf4cdf447a6617a368769`, `sha256:d1008a9ff2f8f26d1f50b3ca587177fa9da8318ef1cbd6dde12a58eb9996d470`, `sha256:22e5fcb36689f4b31b06242ebd161f4cc902acc1ad1ef61dc1d4abfd5c8e7a7d` | `sha256:fd6ea78210f4503fbdfc4463e0c9712ca66ff56b65d1a68029a2dcb54719906e`, `sha256:07bf87bdd7e80b0982c92be553e393a54f33d10a5b8778686fe5e586286851f0` ([run 34784620182](https://github.com/giantswarm/kagent-upstream/actions/runs/34784620182)) |
| `0.11.0-gs.13` | `v0.11.0-gs.13` (2026-09-14T16:08Z, on the rebase-merged giantswarm/kagent-upstream#35 — kagent-dev/kagent#2739 carried as a cherry-pick (`52512ed5`; giantswarm/kagent#57): the Go ADK accepts a `ModelConfig` with `provider: OpenAI`, an `openAI.baseUrl` and no key source — the client sends no `Authorization` header instead of a golden boot that dies with `OPENAI_API_KEY environment variable is not set`; without a `baseUrl` the key stays required. Cut after the agentlab proof of the branch's Go ADK (`golang-adk:dev-4268466d`, swapped in through `platform.devImages.harness` on agent-platform 4.12.3): the keyless template that crashed its golden boot once a minute on the gs.12 Harness reached `Ready=True` on the patched one and `agentlab turn` answered `pong` in 3.97 s; the pin and the other carried patches are those of `gs.12`) | `8d917840` | `6a6b54ff` (2026-09-10) | `sha256:7659f60b53886ec253adb7ebf7ad61fbcc9f5ad353ab2970e6298efd3797883b`, `sha256:8cb004f1e089be9e2eda3c9876b84c4027af778b94b8c1067b3a4a890b721e17`, `sha256:c01d9aa87136cc1a018f6580ed5c31040e1df25d185aa7032ae7e6fc762e1bbe`, `sha256:9383c4f8996803232ee4fbcfaa7e6f9b03548b40d06199a6aabc6027e876eb09` | `sha256:1ebe1d49925ec383e280aa21024e49a167b35037419a033f4c7ac03524a605ff`, `sha256:b8da9df70c1491932214e3695ed8d1dc3d83deefb8eaf7e32cb990732aed3fb9` ([run 34866691831](https://github.com/giantswarm/kagent-upstream/actions/runs/34866691831)) |
| `0.11.0-gs.14` | `v0.11.0-gs.14` (2026-09-14T21:35Z, on the rebase-merged giantswarm/kagent-upstream#37 — the Substrate pin moved from the re-pin candidate's dev build to the line's release `0.0.30-gs.1` — on top of the 2026-09-14 re-pin onto upstream `main` `800015de` (kagent-dev/kagent#2802: Substrate v0.0.29 and the `ate-target-actor` header; the #2739 cherry-pick fell away). The third step of the three-line move (agentgateway `v1.5.1-gs.4` → Substrate `v0.0.30-gs.1` → this): cut after the agentlab proof of the three lines' builds together on agent-platform 4.15.2 as an upgrade from gs.13 / 0.0.27-gs.9 / v1.5.1-gs.3 — `platform-test` 7/7, `agents-test` (create → Ready on the new Harness and the 0.0.30 worker in 18 s → turn), `skills-test` public, `frontmatter-fields` and private with `credentialRef`, `a2a-test` 5/5, a unary `SendMessage` outliving its 5 s caller deadline (`DeadlineExceeded` → `WORKING` → `COMPLETED`, the next message accepted), `toolsets-test` 11/11 — and re-proven on the released Substrate with this commit's own dev build (`0.11.0-dev.giantswarm.2026-09-14.21-16-45.h0a89197`: `platform-test`, `skills-test`). The published chart stamps `substrateWorkerPool.workerImage: ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.30-gs.1`. A first tag of the same name was cut and deleted minutes earlier (its publish cancelled after the `ui` image; the run below is the release): the meta chart's range `>=0.11.0-gs.12 <0.11.1-0` admits gs.14 on every installation the moment the chart exists, so the fleet's kagent sources were suspended until each installation's Substrate had moved) | `0a891975` | `800015de` (2026-09-14) | `sha256:c6f11d2598da141ec4eb80c427d2d9b33160185ee13b821a198051941752d09b`, `sha256:d42c71b5e99d7a9e54a500c44189ada515ecc7a06c86fcbe0f484b44c4fd2a55`, `sha256:78f999768e480a9c454bb9472a69273a8ffe5d8dd97d761cc0dd746966da0f0c`, `sha256:6c65501ca779d613fc3a12033595bef51ca858ad6d4cae56da8d982286a6c57d` | `sha256:5e1697cf89b79f395a5885970ced681115c088ba2abe2361652c363682310bb7`, `sha256:30f5d6ccef6af3c4f43f5d13d5548b4edd1beecb512be99883df8799cedc8196` ([run 34899439294](https://github.com/giantswarm/kagent-upstream/actions/runs/34899439294)) |
| `0.11.0-gs.15` | `v0.11.0-gs.15` (2026-09-15T06:00Z, on the rebase-merged giantswarm/kagent-upstream#39 — kagent-dev/kagent#2788 carried as a cherry-pick of its two commits (`6d3237df`, `3e44b3f7`): `spec.anthropic.promptCaching` / `cacheTTL` on the ModelConfig CRD and three `cache_control` breakpoints in the Go ADK — the last tool definition, the last system block, the last content block of the latest turn — so every model call after a turn's first reads its prefix from Anthropic's cache (giantswarm/giantswarm#37788); the pin and the other carried patches are those of `gs.14`. Cut after the agentlab proof of the dev build `0.11.0-dev.giantswarm.2026-09-15.05-47-53.h138ed1d` through the meta chart's dev channel on agent-platform 4.15.5 (`platform-test` 9/9; a five-call tool turn on a ~4.3 k-token prefix with `promptCaching: true` on the default ModelConfig: the first call wrote 4302 tokens to the cache and every later call read 97-99 % of its prompt from it — 4302, 4410, 5333 — against nothing on gs.14; agentgateway's LLM listener passes `cache_control` through) and, before that, of the branch's own controller and Go ADK builds. The knob is off in the chart, as upstream; the platform's meta chart turns it on (giantswarm/agent-platform#464)) | `138ed1dd` | `800015de` (2026-09-14) | `sha256:9336f29dca1f601a06b4161f1f7bd40964c0b50321ff904ad533f3bc6d847fa6`, `sha256:70acca169bc5e0ad2c7f3f2c13c7cd79d23581dda2c0d6d4546c34ac4c610ff0`, `sha256:c338b48030d9ccd643251b4417593c694191eda82b083253c0473719baa27bd3`, `sha256:4f8b017f20bf16194a8b4cbfdf38cb522e3e96125815ea5449ab5bccf419d26b` | `sha256:991dc1a1a9f2d033c6c14252bedcafb75e01462f46912f08a29b5c3e68eacc17`, `sha256:2f0f88f3d29076a170b48546c3d4817cddaaa25131302595ed363a6e20eec24f` ([run 34934918676](https://github.com/giantswarm/kagent-upstream/actions/runs/34934918676)) |
| `0.11.0-gs.16` | `v0.11.0-gs.16` (2026-09-15T09:02Z, on the rebase-merged giantswarm/kagent-upstream#41 — kagent-dev/kagent#2790 carried as a cherry-pick of its four commits (`e66fb5a9` the ADK v2.3.0 bump, `229ae568` the Go runtime honouring `context_config.compaction`, `cc11cc96` `spec.context.compaction` on the AgentTemplate CRD with its CEL rules, `646e7fe4` the mockllm e2e): tail retention and the sliding window of ADK's `session/compaction` reach the platform's Go ADK actors (giantswarm/giantswarm#37792); the pin and the other carried patches are those of `gs.15`. Cut after the agentlab proof of the branch's dev build `0.11.0-dev.feat-context-compaction-2790.2026-09-15.08-28-06.hd708902` (a tree identical to this commit) pinned as the meta chart's kagent and kagent-crds components on agent-platform 4.16.0 with Substrate 0.0.30-gs.1: `platform-test` 9/9; two AgentTemplates rendered by the agent chart's `context.compaction` values (24 000 / 4 and 8 000 / 4) admitted with the field, Ready in ~25 s, their golden boots logging `context compaction enabled …`; a tool-heavy turn on each had the summariser fire once at the threshold (1.9 k in / 0.5 k out in 10.9 s; 1.1 k / 0.4 k in 8.3 s) before the model read a 43 k / 57 k-token listing, both answers complete. Off unless a template sets it, as upstream; the platform's agent chart 1.3.0 sets the default (giantswarm/agent#38), the meta chart moves its range to this release (giantswarm/agent-platform#471)) | `b692efd7` | `800015de` (2026-09-14) | `sha256:c614a46902283ed1744c50a635a9098172b9c6e37bc910eb30e874ddbcc088a7`, `sha256:591e9ab0502b330ee7c33e695bf78440e00607089204d70c0193bba56a45b2ab`, `sha256:c083c1b44c3b84b6b0a92dc0a0c714bbd569636d1b43c4002b3dd367fbad765e`, `sha256:a7e3a9e7400b123061a7563dae23509a240c2563b79adcbb6364f08fb73fe018` | `sha256:06749d6e2ba015e8310a7a4de10c8eb1a137afcf964ebb90b494b0e418935cc1`, `sha256:56bdb3a3ee80f72a1c8a084402ed5464adcb4a24c112abe7de2e6a51f0fa9709` ([run 34950314531](https://github.com/giantswarm/kagent-upstream/actions/runs/34950314531)) |
| `0.11.0-gs.17` | `v0.11.0-gs.17` (2026-09-15T11:09Z, on the rebase-merged giantswarm/kagent-upstream#43 — `e821fcdc` `fix(a2agateway): end a turn whose runtime is lost, and delete a paused or crashed Actor as it is`: after a failed runtime stream the gateway asks Substrate for the Actor and, when it is `CRASHED` or gone, fails the task with `runtime lost: <cause>`, moves the AgentInstance to `FAILED` with `failure.reason` `RuntimeLost`, refuses later sends with that message without dialing, and no longer asks the runtime whether it took the task; `Delete` suspends only a live Actor and deletes a `PAUSED` one through `any_state` (giantswarm/giantswarm#37795, #37742 row 45); the pin and the other carried patches are those of `gs.16`. Cut after the agentlab proof of the branch's controller build on agent-platform 4.16.0 with Substrate 0.0.30-gs.1: a turn parked on a `requireApproval` tool, its Actor record rewritten to `CRASHED`, the answer failed after one atenet parking budget (30 s, was 60) with `runtime lost: Actor kagent/ai-… crashed: …`, the instance read `FAILED`/`RuntimeLost` over gRPC, the next send was refused in 10 ms, the delete took 20 ms with no `SuspendActor`; a paused Actor pinned to a gone node (the gazelle record) deleted in 10 ms with `any_state`; a healthy turn completed; `platform-test` 9/9) | `d990b84d` | `800015de` (2026-09-14) | `sha256:757920cccf1a6a24c89ee88139e80942c18f783ab4a168bb704f9ff549da4c69`, `sha256:edb4c38d792d44e205b56646a1fa7928c992d1fc64c1c6c8a45f684199a38efe`, `sha256:fad227d8732c3c42cffb736e61d8b3de023208aa2dea078cadf90728a3c74866`, `sha256:04512e0e71b6b4600d6197be36f557ec96eb404e389bb647fbb8a4c5e17fa8cb` | `sha256:c8c752a6f376618cc9b4fe2ecce68c3d0ab00df11aadd16a4efa396643aeb138`, `sha256:0304f34b9ba1359dcd9d3b307423127f3d3ccc503c5a8e17094d74c41f7ef06d` ([run 34961733905](https://github.com/giantswarm/kagent-upstream/actions/runs/34961733905)) |
| `0.11.0-gs.18` | `v0.11.0-gs.18` (2026-09-15T13:35Z, on the rebase-merged giantswarm/kagent-upstream#44 — `f001a44f` `feat(a2agateway): suspend a runtime paused for input once the pause is older than a TTL`: the controller's leader sweeps READY instances whose latest task waits for input without a recorded snapshot and, once the pause is older than `KAGENT_PAUSED_RUNTIME_TTL` (2 m; `controller.pausedRuntimeTTL`; 0 disables), `SuspendActor`s the paused Actor and records the snapshot as the task's boundary (`RecordAgentInstanceTaskSnapshot`, guarded on the task still waiting); the gateway dispatches under the runtime-call lock and the sweep takes the quiesce lock without waiting (giantswarm/giantswarm#37795, #37742 row 45); the pin and the other carried patches are those of `gs.17`. Cut after the agentlab proof of the branch's dev build `0.11.0-dev.feat-pause-ttl.2026-09-15.10-19-04.hb4491aa` pinned as the meta chart's kagent and kagent-crds components on agent-platform 4.16.0 with Substrate 0.0.30-gs.1 and the TTL at 90 s: the two sessions the lab had parked on questions for 45 h and 14 h were suspended within a second of the controller start (`SuspendActor`, `external_snapshot`, tasks still `input-required` with the boundary recorded, node checkpoints gone); a probe parked on `ask_user` was suspended 109 s after its pause; with every worker replaced and the actor's node directory wiped, the answer restored it on a fresh worker from the store (`data_on_golden`, download 19 ms) and the turn completed with the transcript intact; an answer 20 s into a pause resumed in place; controller log 0 errors, `platform-test` 9/9) | `fbab147c` | `800015de` (2026-09-14) | `sha256:f9f2f803e2027d691e336f96f157800635535d3460b3072012e5914b67e5d44e`, `sha256:70e100e94fbcb9e4dd583f5c129c46eadd3ef7bba2f3c16c897c4b941c9c6761`, `sha256:1567afac4744b080efe09e2a19f8dd6968a49c918c1240bb389ed2b04effd620`, `sha256:761720a51bca0459433880455a165715eead7dd82c49374c33d9bd2f856b540f` | `sha256:86a0f3e0b23435686f4fe3d7086c2a53959aa266da53bd6ec09fcea2d2550fc4`, `sha256:896640ff898c4ed43767cea958d662625aa65fdb02b80b6d5a3f72d9014ee0d0` ([run 34976009655](https://github.com/giantswarm/kagent-upstream/actions/runs/34976009655)) |
| `0.11.0-gs.19` | `v0.11.0-gs.19` (2026-09-15T14:56Z, on the rebase-merged giantswarm/kagent-upstream#47 — `3b665b18` `fix(a2agateway): leave a pause whose node has no worker alone, and delete a runtime that cannot be suspended as it is`: the pause TTL's first sweep on graveler (2026-09-15 13:46Z) met three paused Actors whose checkpoint nodes were gone — Substrate fails the upload at once and leaves the Actor `SUSPENDING`, which the lost-runtime patch's `Delete` then suspends first and fails the same way, so the sessions were not deletable. `Idle` counts a `PAUSED` Actor only while a worker runs on its checkpoint's node (`ListWorkers`), so the sweep leaves a pause on a lost node to the node-loss handling (giantswarm/giantswarm#37742 row 44); `Delete` falls back to an any-state delete when the pre-delete suspend fails (giantswarm/giantswarm#37795); the pin and the other carried patches are those of `gs.18`. Cut after the agentlab proof of the branch's dev build `0.11.0-dev.fix-pause-ttl-lost-node.2026-09-15.13-54-14.hd435f11` on agent-platform 4.16.0 with Substrate 0.0.30-gs.1, the TTL at 60 s and the Actor records rewritten to a gone node: three sweeps after the TTL did `GetActor` + `ListWorkers` only and the Actor stayed `PAUSED`; the node name restored, the next sweep suspended it; a `SUSPENDING` Actor on a gone node was deleted in 22 ms — `SuspendActor` failed with the graveler error, `DeleteActor any_state=true` succeeded; controller log 0 errors, `platform-test` 9/9) | `2e18f2b4` | `800015de` (2026-09-14) | `sha256:e937231facda222b3849664208a676fb08cd76918778c7abd3de6b24d6ec6f13`, `sha256:09a69bf72a87fc01ae167ebfea520c11ae70cabb59c1746d6e1fc0c860c2fa63`, `sha256:5c1f3b12321b90a2577787d376fae9b82f5488dc0644907d9130a989428b4d69`, `sha256:7cda86a719ebad84f41150aca318893ff49f52fe7d45721aa184f6a0bbfe76cd` | `sha256:bd4423be4d9e22bf62ddf05fab3c9e5a6eac2031d4d31380121defb09ad8822d`, `sha256:4da498de15fed904c35a6ae45815bd51e2e9a941306294d54ab9fbe78a6bb7b4` ([run 34984968970](https://github.com/giantswarm/kagent-upstream/actions/runs/34984968970)) |
| `0.11.0-gs.20` | `v0.11.0-gs.20` (2026-09-15T19:14Z, on the squash-merged giantswarm/kagent-upstream#49 — Substrate re-pinned from `0.0.30-gs.1` to the line's release `0.0.30-gs.4`, which carries giantswarm/substrate#39: the golden boot a workload keeps failing is bounded — ate-api counts the boots the workload itself fails (`WORKLOAD_NOT_READY`) in `GoldenSnapshotStatus.workload_boot_failures`, at the third crashes the golden actor (worker freed) and fails the ActorTemplate with `GoldenActorNotReady`, the bound and the last boot's error, and the worker `ateom-gvisor:0.0.30-gs.4` this chart stamps quotes the workload's last output lines in the readiness-deadline error; this controller surfaces the message verbatim as `Ready=False ActorTemplateFailed` (giantswarm/giantswarm#37801); the pin and the carried patches are those of `gs.19`. Proven on gazelle 2026-09-15 19:16–19:20Z after the roll: a private-skill template `ActorTemplateFailed: GoldenActorNotReady: … last output of "kagent": … fatal: unable to access 'https://github.com/giantswarm/claude-code/' …` 3 min 9 s after creation, collected on delete) | `f3eced68` | `800015de` (2026-09-14) | `sha256:468c26a60d83633bf509472267d52dd3a5598a86d59623edb5c6800f2c5c81b2`, `sha256:77075fdb285531c5216000a902b60c9806cadd4b7f59de6f5d28f5e88f74b09e`, `sha256:a4a3342d97edbef37367577064c9887ff120b81538dfd579ec47c07e0c3b19c8`, `sha256:91720ca82eebc03799da0dcf7956a3ef113b6505e5fc32ffe060cb14d010da1d` | `sha256:85eda33f377c791286afe4e60c632cf950484cf5869874aeeebbb9f4445470f2`, `sha256:0d42c8228b7944c9c57f844072cee2747a08ba6b05257a55e812e56671f6bf9b` ([run 35011669868](https://github.com/giantswarm/kagent-upstream/actions/runs/35011669868)) |
| `0.11.0-gs.21` | `v0.11.0-gs.21` (2026-09-19T00:32Z, on the rebase-merged giantswarm/kagent-upstream#52 — `6213c182` `feat(runtime): name the agent and the user on every model call`: every model call an actor makes carries `x-kagent-agent` and `x-kagent-agent-namespace` (the `AgentTemplate`'s name and namespace; the controller injects `KAGENT_AGENT_TEMPLATE`, the Go ADK's HTTP transport sets them after a ModelConfig's default headers, the Claude harness sends them through `ANTHROPIC_CUSTOM_HEADERS`) and `x-kagent-user`, the person of the turn — forwarded by the controller's a2a gateway only for a session whose identity the trusted-proxy authenticator resolved from the caller's validated token, read by the ADK from that metadata alone (giantswarm/giantswarm#37871, #37742 row 48); the pin and the other carried patches are those of `gs.20`. Cut after the agentlab proof of the branch's dev build `0.11.0-dev.feat-model-call-identity-headers.2026-09-19.00-03-55.hfea51de` (a tree identical to this commit) with both the controller and the Harness swapped on agent-platform 4.40.0 and the default ModelConfig routed through the `llm` listener: one turn of an `AgentTemplate` as a lab user produced `agentgateway_gen_ai_client_*` samples with `agent`, `agent_namespace` and `user` read from the headers (the person's email, never the token the two earlier builds had leaked through a context key colliding with the bearer's), `platform-test` 12/12; the container proofs of each build are on the issue. Roll note: the 4.x installations follow the chart from ghcr by range but pull the images from the gsoci mirror, which retagger fills for `-gs.N` tags on its own runs, so the fleet's `kagent`/`kagent-crds` OCIRepositories were suspended until gsoci had this release's controller tag and Harness digests, then resumed one installation at a time) | `7c0e5743` | `800015de` (2026-09-14) | `sha256:85230981ca0164603aeb5859ede4bc12e9ba461da50bf8f6ccd0997303cae472`, `sha256:cb590f490bb731acce21d378db5e3a762b66bcc4533a3a7f0262c496529f2c1c`, `sha256:18d8c2b47f5d91aa33526f456dda99eba78d83f9b83a75f233261ab5642fe8e4`, `sha256:87cd3e8945edc51b263d7c0f927f39d9fcdbab3128ed6896931df03e8151bf83` | `sha256:547339238318fa3ad603d12ce64d11d15ccc95c32618034decc1d3a1e89d284f`, `sha256:912cd4a0577cc05805678dead45b0120ad4694d10546e90624157d24a642bc2d` ([run 35409682239](https://github.com/giantswarm/kagent-upstream/actions/runs/35409682239)) |
| `0.11.0-gs.22` | `v0.11.0-gs.22` (2026-09-19T05:52Z, annotated, on the rebase-merged giantswarm/kagent-upstream#55 — the first release published to gsoci from CircleCI: the four images under `gsoci.azurecr.io/giantswarm/kagent` and both charts under `oci://gsoci.azurecr.io/giantswarm/kagent/helm`, cosign-signed under the project's CircleCI identity with per-platform SPDX attestations; `tag.yaml` and every push to ghcr.io gone ("What is published"). Code since gs.21: giantswarm/kagent-upstream#54 (the pair collections' outputs compared by content). Cut after the agentlab proof of the branch's dev build `0.11.0-dev.fork-gsoci-circleci-publish.2026-09-19.04-39-44.h3de604c` on meta chart 4.40.0 with the consumer's kagent sources pointed at the gsoci paths — `platform-test` 13/13, `agents-test` (golden boot of the gsoci runtime image on the Substrate worker, a turn, a skill re-pin Ready in 12 s, delete). The published chart stamps `registry: gsoci.azurecr.io`, the `giantswarm/kagent/*` repositories and `substrateWorkerPool.workerImage: ghcr.io/giantswarm/substrate/ateom-gvisor:0.0.30-gs.4` — the Substrate line's path until the re-pin to its `0.0.30-gs.5`. Consumers on the ghcr chart path still resolve gs.21; the switch is theirs) | `c371e27f` | `800015de` (2026-09-14) | `sha256:a3c86b4b8f7724cf3ea8a0874721f6995003313ed99dbffee670bbe99ae01b46`, `sha256:8789252c85f4d8e615ca3a55326431fd96f61e5513131c47d9997e4aabaaa392`, `sha256:8052c057198a33cb7610a8fb5f3f73c66d60755a32aaba5fe2a78278d74bca5b`, `sha256:9496d92047734990c2210a5c37cbab7db22330d9c124258ea2cedef883e8d8b1` | `sha256:10d83411f156b098f50adc6eca88b3129eaf5d473f3fefe336f0ba68d1a88564`, `sha256:9dbd6c80820dc17ac2adf94f8fbf9d5f24218fe13b1bc02ce15f29d14fbc2460` ([pipeline 12](https://app.circleci.com/pipelines/github/giantswarm/kagent-upstream/12)) |
| `1.0.0` | `v1.0.0` (2026-09-19T14:59Z, annotated, on `e09e3416` — the rebase-merged giantswarm/kagent-upstream#58: the first release of the decoupled scheme ("Release scheme"), published by the orb's stock jobs — two native `build-image` jobs per image and a `push-to-registries` merge — from CircleCI pipeline 29, 5 min 49 s from the tag to the ledger row. Code since gs.22: the Substrate re-pin to the line's `1.0.0` (the runtime of `0.0.30-gs.5`, its router and egress on the agentgateway line's `2.0.0`) and the truncated label values fix (`72efa7ea`). Cut after the consumed branch's first stock dev build of the same tree (pipeline 27) published green; the platform proof of the release is the consumer's move to the `>=1.0.0 <2.0.0` range on the same OCI paths (giantswarm/agent-platform#580). The published chart stamps `registry: gsoci.azurecr.io`, `tag: "1.0.0"`, the golang-adk and claude-harness index digests below and `substrateWorkerPool.workerImage: gsoci.azurecr.io/giantswarm/substrate/ateom-gvisor:1.0.0`, and depends on `oci://gsoci.azurecr.io/giantswarm/substrate/helm/{substrate,substrate-crds}:1.0.0`; every index carries `io.giantswarm.upstream.version=main@800015d`) | `e09e3416` | `800015de` (2026-09-14) | `sha256:35e02911720d87aa1d0c7512d4e4233168bdbdaf46b31d3b93ccbd291cf1ec0a`, `sha256:742b0d4db97ebb11310353c90a022ad9e1c08c6855eaf01b501f398dcae1d8a1`, `sha256:d185a24f7c73397561de37631058f20c31d15ea4c9e6b5f23cc9933000b0a1f9`, `sha256:fac3c48a281122fcbad7d89ceaee97fd084f1a3c8f592e43cde0a74aa549d1cc` | `sha256:0f137e3e9df1e9c9226072763e5a44a969ac80aab7367e61a4aae5c957f2cc2e`, `sha256:e2c15365fc30e8d3f0cec80e19713d24bcd7fc28ec28926ea5d9028de4ca9f3a` ([pipeline 29](https://app.circleci.com/pipelines/github/giantswarm/kagent-upstream/29)) |
| `1.0.1` | `v1.0.1` (2026-09-22T09:46Z, annotated, on `e366fda5` — the merged giantswarm/kagent-upstream#61: the controller serves Prometheus metrics at all. `app.Run` built the manager with `metricsserver.Options{BindAddress: "0"}`, which no variable changed, so upstream's own ServiceMonitor (kagent#2401) targeted a port the binary never bound; and `grpcserver.Config.Registerer` was nil, so `kagent_grpc_server_requests_total` and `..._request_duration_seconds` were recorded on every call and registered nowhere. `METRICS_BIND_ADDRESS` (default `"0"`, which keeps the server off — controller-runtime reads an empty address as unset and would fall back to `:8080`) and `METRICS_SECURE` (the `TokenReview` and `SubjectAccessReview` filter) now configure it, and the gRPC interceptors register on controller-runtime's registry. Upstream: kagent-dev/kagent#2886, open — the two commits are carried until it merges and the next re-pin drops them. No re-pin: the tree is `1.0.0` plus those two commits, so every installation on the range `>=1.0.0 <1.1.0` takes the release with the metrics server still off until a consumer sets the variable) | `e366fda5` | `800015de` (2026-09-14) | `sha256:02d3896007d26d2220c4b9c94bffea778bd3c7498e53d3ef96fbe8ff68b94813`, `sha256:425888f7279bb7969654ca23f3b341cead32baa04392d96563400e9717ff34b3`, `sha256:78726065f429e67dd3f49703e72d0ab17f3fa15a412edeecf790a786c769a069`, `sha256:81ee41c175c72f6d643126ba9509bbc3a73c2e2bee1bb0b8787fdee76aa2e4b3` | `sha256:5bb4fa61c0c25b95fb5a73b43569c3149e01f5426ca59d2090dd3d4cd9b4fe60`, `sha256:eddc875d9bcc9e448ee39042abb2fde6bd032414a31bf93cc6090af341996ec0` |

## CI and security

`ci.yaml` (upstream's suite: Go unit tests and lint, Helm unit tests, protobuf contract check, manifests check,
UI typecheck/lint/unit/browser tests, Python tests and lint, multi-arch image builds, the e2e against kind +
Substrate, the upgrade legs) and `image-scan.yaml` (Trivy on every published image at CRITICAL/HIGH,
`govulncheck` over the Go module graph) run on every push to `giantswarm` and `sync/**`, on every pull
request to `giantswarm`, and — the scan — weekly. Fork differences from upstream's files: the triggers,
GitHub-hosted runners with a `docker/setup-buildx-action` builder instead of Blacksmith runners and their
builder, the image matrices trimmed to the published set, no `paths-ignore` (a docs-only pull request must still
report the required checks), and one aggregate job per workflow — `ci-ok`, `scan-ok` — which the ruleset requires.

Security findings are handled by the team's policy: a fixable finding is fixed (a dependency bump is a patch
like any other — upstream pull request first, carried here until merged; an image fix goes into the Dockerfile
the same way, as the two `apk upgrade` commits above). A finding upstream cannot fix yet, or a scanner
false positive, gets a time-boxed, justified ignore — `.trivyignore` with Trivy's native `exp:YYYY-MM-DD` and a
comment naming the module and the reason — so the line keeps moving; it is re-reviewed at the expiry. `govulncheck`
has no ignore file of its own, so `scripts/fork/govulncheck.sh` applies `.govulncheck-ignore` (`GO-… until=… # why`)
the same way: an expired entry fails the scan until it is re-reviewed, and only a finding with no fixed version
may be listed there.

Upstream automation the fork does not need is off: Dependabot version updates are disabled on this fork (a
fork's default; dependency updates arrive through the re-pin), `stalebot`, `conventional-label` and
`label-pull-requests` are disabled workflows (repository setting, not a file edit), `migration-immutability`
only fires on pull requests to `main`/`release/**`, which do not exist here. Upstream's workflow files do start
when the mirror is pushed (the push has to be made as the App — see "The mirror"); the sync cancels those runs
within seconds, so a cancelled run per mirrored branch or tag in the Actions tab is expected, not a failure.

## Re-pin

The line is rebased onto upstream `main` weekly (Monday 05:00 UTC) by `.github/workflows/sync-upstream.yaml`,
also on demand (`gh workflow run sync-upstream.yaml`, inputs `upstream_ref`, `dry_run`). One run:

1. fast-forwards the mirror — `main`, the `release/v*.x` branches, the tags — to upstream (`scripts/fork/mirror.sh`;
   refuses if a mirrored branch was edited; never moves or deletes a ref) and cancels the runs upstream's own
   workflow files start at the mirrored refs (`scripts/fork/mirror-quiet.sh`);
2. rebases the carried commits onto the upstream head into a candidate branch `sync/upstream-<date>-<sha7>`
   (`scripts/fork/repin.sh`; commits upstream merged in the meantime are dropped and named in the report,
   with the upstream commit they landed as);
3. pushes the candidate (as the org's `heraldbot` GitHub App, whose token the run mints — a push by an App
   triggers workflows, a push with the workflow's own token would not); `ci.yaml` and `image-scan.yaml` run on it;
   the workflow waits for `ci-ok` and `scan-ok`;
4. green: force-pushes the candidate onto `giantswarm` (`--force-with-lease` on the head it started
   from), deletes the candidate, appends the row to `re-pins.md`; the pipeline publishes the dev build and
   appends its row to `builds.md`;
5. a conflict or a red check: nothing is pushed to the consumed branch; a pull request against
   `giantswarm` names the conflicting carried commit (with the files) or the failed checks, and the
   candidate branch stays for the fix. Finish by hand, then close that pull request — never merge it (a merge
   would keep the old base).
6. the pin annotation `index:io.giantswarm.upstream.version=main@<sha7>` in `.circleci/config.yml` names the
   upstream pin on every published image ("What is published") and the sync does not edit it: after a landed re-pin,
   a pull request against `giantswarm` moves it to the new pin (its own commit — a pull request cannot fix up the
   `ci(fork)` carried commit; a manual re-pin may fold it). `record` compares the stamped annotation with
   `git merge-base` on every build: a dev build whose annotation lags warns, a release whose annotation disagrees is
   refused before its ledger row.

A re-pin is proven before anything depends on it: the agent-platform dev line re-pins the new build's tag and
`Harness` digests, and agentlab on the dev channel runs its proofs from the published artifacts.

### Manual re-pin

The same steps by hand, for when the workflow is broken or a conflict needs a human:

```bash
git clone https://github.com/giantswarm/kagent-upstream && cd kagent-upstream
git remote add upstream https://github.com/kagent-dev/kagent.git
CANDIDATE=sync/upstream-$(date -u +%Y%m%d)-manual scripts/fork/repin.sh   # exit 2 = conflict, report in repin-report.md
# on a conflict: git rebase --onto upstream/main "$(git merge-base origin/giantswarm upstream/main)" ; fix ; git rebase --continue
sed -i "s|upstream.version=main@[0-9a-f]*|upstream.version=main@$(git merge-base HEAD upstream/main | cut -c1-7)|" .circleci/config.yml && git commit -qsam 'chore(fork): upstream pin annotation'   # the pin annotation moves with the pin (step 6); fold it into the ci(fork) commit if you rebase interactively
git push origin "$CANDIDATE"                      # ci.yaml + image-scan.yaml run on sync/** ; wait for ci-ok and scan-ok
git push --force-with-lease=refs/heads/giantswarm origin "$CANDIDATE":giantswarm
git push origin --delete "$CANDIDATE"
scripts/fork/mirror.sh "$(gh auth token)" && scripts/fork/mirror-quiet.sh "<ISO time before the push>" mirror-pushed.txt   # or: gh workflow run sync-upstream.yaml -f mirror_only=true
scripts/fork/ledger-append.sh re-pins.md '| <date> | <old pin> | <new pin> | <old head> → <new head> | <replayed> | <dropped> | manual |'
```

The force-push needs a bypass of the consumed branch's ruleset, which only the automation (the `heraldbot`
App) has: an administrator adds themselves to the bypass list for the duration and removes themselves right
after (the same discipline as lifting `enforce_admins` for a guarded merge). Never `--force` without
`--force-with-lease`.

### The mirror

`main`, the `release/v*.x` branches and the tags are fast-forwarded by the sync as the App (`scripts/fork/mirror.sh`;
`protect-mirror-main` ruleset on `main`: no deletion, no force-push). The workflow's own token cannot be used:
GitHub refuses any push that creates or updates a file under `.github/workflows` without the `workflows`
permission, mirrored upstream commits included, and `GITHUB_TOKEN` never has it. An App push starts upstream's
workflow files at the mirrored refs; `scripts/fork/mirror-quiet.sh` cancels them right after. Upstream's CI
resolves the upgrade-test baseline from the fork's own release branches and tags, so the mirror must carry them
(the fork's own release tags `vX.Y.Z` share upstream's shape and live in a major above upstream's — "Release scheme"; `mirror.sh` refuses to mirror an upstream tag whose name the fork already holds at another commit, so a collision stops the sync, naming the tag, instead of one side silently winning).
If a mirrored branch was ever edited, the sync refuses to fast-forward it; restore it with
`git push --force-with-lease=refs/heads/<branch> origin upstream/<branch>:<branch>` after lifting the ruleset
briefly, then run the sync again. `gh workflow run sync-upstream.yaml -f mirror_only=true` mirrors without
re-pinning.

## Branch protection

Ruleset `protect-consumed-branch` on the default branch: no deletion, no force-push, changes only through a
pull request with one approval, required status checks `ci-ok` and `scan-ok` on an up-to-date branch. Pull
requests are **rebase-merged, never squashed**: every commit on the consumed branch is one carried patch with
its own upstream exit, and the re-pin drops a commit only when its patch matches an upstream commit — a squash
would weld a cherry-picked upstream fix to unrelated changes and lose that.
Bypass: the `heraldbot` GitHub App (the org's automation App, installed on every repository; the sync
pushes with a token it mints from the org secrets) always — that is the re-pin's force-push; repository
administrators for pull requests only (merging an own green pull request without waiting for the approval).
Ruleset `protect-mirror-main` on `main`: no deletion, no force-push. Teams `team-bumblebee` and `bots` have
write access.

## Contributing

- **Every patch is an upstream pull request first** (kagent-dev/kagent, DCO sign-off on every commit; a
  workflow run on a pull request from a fork waits for a maintainer's approval). The fork commit and the
  upstream pull request are the same change and reference each other. Work on a branch of this repository
  (`fix/…`, `feat/…`) and open the pull request here against `giantswarm` with the upstream link; the
  upstream pull request is opened from this repository's branch too, so the same commit serves both.
- **A personal fork** is for exploration that may never be proposed; the moment a change is meant for the
  platform it moves here.
- **A patch lives here until upstream merges it**; the next re-pin drops it. A patch upstream rejects is
  either re-proposed in the shape the maintainers want or recorded in the table above as a deliberate
  permanent difference with the reason — the target is none.
- Anything Giant Swarm-specific (Substrate wiring, platform `Harness` objects, registries, security contexts)
  belongs to the agent-platform charts, not to this line.
- Never commit to `main` (the mirror), `sync/**` or `ledger`.

## Ledger

Machine-written, on the `ledger` branch: [`builds.md`](../../blob/ledger/builds.md) — one row per published
build (version, commit, upstream pin, every image and chart digest, the run); [`re-pins.md`](../../blob/ledger/re-pins.md)
— one row per re-pin (previous and new pin, old and new head, replayed and dropped commits, the run).
Human-written, here: the tables above. Together they answer "which kagent are we running" without opening an
issue tracker; giantswarm/giantswarm#37010 and #37742 hold the wider context.
